{
  "name": "@openci/api",
  "version": "0.0.0",
  "main": "lib/index.js",
  "license": "MIT",
  "scripts": {
    "start": "tsc --watch",
    "lint": "tslint 'src/**/*.ts'",
    "lint:fix": "tslint 'src/**/*.ts' --fix",
    "test": "jest --coverage",
    "build": "tsc",
    "prepublishOnly": "yarn build"
  },
  "dependencies": {
    "@openci/spec": "*",
    "ajv": "^6.10.0"
  }
}
