language: node_js
node_js: 10
stages:
  - test
  - publish
jobs:
  include:
    - stage: test
      script:
        - yarn
        - yarn build
        - yarn test
    - stage: publish
      script:
        - yarn
        - yarn build
        - yarn publish

