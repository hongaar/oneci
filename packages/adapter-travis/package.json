{
  "name": "@oneci/adapter-travis",
  "version": "1.0.0-alpha.1",
  "main": "lib/index.js",
  "files": [
    "lib/*"
  ],
  "license": "MIT",
  "scripts": {
    "lint": "tslint 'src/**/*.ts'",
    "lint:fix": "tslint 'src/**/*.ts' --fix",
    "test": "jest --coverage --passWithNoTests",
    "test:watch": "jest --watch",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "prepublishOnly": "yarn build"
  },
  "dependencies": {
    "@oneci/adapter-base": "^1.0.0-alpha.1",
    "@oneci/spec": "^1.0.0-alpha.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
