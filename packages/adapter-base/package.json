{
  "name": "@oneci/adapter-base",
  "version": "1.0.0-alpha.1",
  "main": "lib/index.js",
  "files": [
    "lib/*"
  ],
  "license": "MIT",
  "scripts": {
    "lint": "tslint 'src/**/*.ts'",
    "lint:fix": "tslint 'src/**/*.ts' --fix",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "prepublishOnly": "yarn build"
  },
  "dependencies": {
    "@oneci/spec": "^1.0.0-alpha.1",
    "ajv": "^6.10.0",
    "got": "^11.8.5"
  },
  "devDependencies": {
    "@types/got": "^9.4.4"
  },
  "publishConfig": {
    "access": "public"
  }
}
