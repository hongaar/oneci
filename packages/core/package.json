{
  "name": "@oneci/core",
  "version": "1.0.0-alpha.1",
  "main": "lib/index.js",
  "files": [
    "lib/*"
  ],
  "license": "MIT",
  "scripts": {
    "lint": "tslint 'src/**/*.ts'",
    "lint:fix": "tslint 'src/**/*.ts' --fix",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "prepublishOnly": "yarn build"
  },
  "dependencies": {
    "@oneci/adapter-base": "^1.0.0-alpha.1",
    "ajv": "^6.10.0",
    "better-ajv-errors": "^0.6.4"
  },
  "publishConfig": {
    "access": "public"
  }
}
