{
  "name": "openci",
  "private": true,
  "scripts": {
    "start": "lerna run start",
    "test": "lerna run test",
    "build": "lerna run build",
    "release": "lerna publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "tslib": "^1.9.3"
  },
  "devDependencies": {
    "@types/jest": "^24.0.12",
    "@types/node": "^11.13.8",
    "jest": "^24.7.1",
    "lerna": "^3.13.4",
    "ts-jest": "^24.0.2",
    "tslint": "^5.16.0",
    "tslint-config-standard": "^8.0.1",
    "typescript": "^3.4.5"
  }
}
