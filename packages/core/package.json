{
  "name": "@oneci/core",
  "version": "0.0.0",
  "main": "lib/index.js",
  "license": "MIT",
  "scripts": {
    "lint": "tslint 'src/**/*.ts'",
    "lint:fix": "tslint 'src/**/*.ts' --fix",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "release": "semantic-release"
  },
  "dependencies": {
    "@oneci/adapter-base": "^0.0.0",
    "ajv": "^6.10.0",
    "better-ajv-errors": "^0.6.4"
  }
}
